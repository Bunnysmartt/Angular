import { ChangetheelementDirective } from './changetheelement.directive';

describe('ChangetheelementDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangetheelementDirective();
    expect(directive).toBeTruthy();
  });
});
